import { CHARCOAL } from '../../styles/colour.config';

export default {
  '.component': {
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    position: 'fixed',
    top: 0,
    zIndex: 1,
    backgroundColor: CHARCOAL,
  },
};
